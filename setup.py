#!/usr/bin/env python

from setuptools import setup, find_packages

setup(
    name="chess-bot",
    version="1.0",
    description="Kaggle machine learning wrappers",
    author="Marius Kuper",
    packages=find_packages(),
)
